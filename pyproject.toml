[project]
name = "text2tok"
version = "0.1.0"
description = "Text tokenizers optimized for sparse retrieval."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "regex>=2025.9.1",
    "transformers>=4.56.1",
]
